import { Cases } from '@/components/website/Cases';

export const metadata = {
  title: 'Cases — BRAPRI Tecnologia',
  description: 'Projetos entregues para indústria, varejo, logística e serviços. Resultados reais medidos depois do go-live.',
};

export default function CasesPage() {
  return <Cases />;
}
