import { Legal } from '@/components/website/Legal';

export const metadata = {
  title: 'Políticas — BRAPRI Tecnologia',
  description: 'Termos de uso, política de privacidade, reembolso e cancelamento, e entrega de serviço.',
};

export default function LegalPage() {
  return <Legal />;
}
