import { Contact } from '@/components/website/Contact';

export const metadata = {
  title: 'Contato — BRAPRI Tecnologia',
  description: 'Solicite uma proposta. Retornamos com escopo, prazo e investimento em até 1 dia útil.',
};

export default function ContatoPage() {
  return <Contact />;
}
